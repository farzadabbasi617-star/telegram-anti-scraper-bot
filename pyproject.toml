[project]
name = "telegram-anti-scraper-bot"
version = "1.4.3"
description = "ربات ضد اسکریپت تلگرام — استخراج ممبر، ادد هوشمند ضد بن، و داشبورد مینی‌اپ"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
asyncio_mode = "auto"
