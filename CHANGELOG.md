# 📜 Changelog

تمامی تغییرات مهم پروژه در این فایل ثبت می‌شوند.
فرمت بر اساس [Keep a Changelog](https://keepachangelog.com/fa/) و نسخه‌بندی معنایی (SemVer).

---

## [1.4.5] — 2026-08-16 — ⚡ علت واقعی «دکور بودن» دکمه ادد: کوئری N+1

نسخه ۱.۴.۴ یک گلوگاه را رفع کرد ولی دکمه همچنان بی‌جواب بود. تست زنده
با curl نشان داد هنوز ۶۰ ثانیه بلاک می‌شود.

**علت اصلی:** فیلتر ضد تکرار به ازای *هر* کاربر یک `db.is_added()`
صدا می‌زد:

```python
for u in raw_users:              # تا ۱۰٬۰۰۰ کاربر
    if db.is_added(target_gid, uid):   # ← یک کوئری برای هرکدام!
```

یعنی تا ۱۰٬۰۰۰ رفت‌وبرگشت جداگانه به Postgres، داخل مسیر همگام درخواست.
چند دقیقه طول می‌کشید و مینی‌اپ تایم‌اوت می‌خورد.

### Fixed
- ⚡ `db.get_added_user_ids(group_id)` — یک کوئری به‌جای ۱۰٬۰۰۰.
  نتیجه در `set` نگه داشته می‌شود تا چک عضویت O(1) باشد.
- هر دو مسیر (تک‌اکانت و موازی) اصلاح شدند.

### Added
- تست نگهبان N+1: هیچ فراخوانی `db.*` نباید داخل حلقه فیلتر ممبرها
  باشد. با تزریق باگ قدیمی راستی‌آزمایی شد.

### Notes
- تست‌ها: ۱۱۳ → **۱۱۸** (همه سبز)

## [1.4.4] — 2026-08-16 — 🔘 دکمه‌های ادد کار افتادند + حذف اکانت

**گزارش مالک:** «صفحه ادد اصلاً کار نمی‌کند و دکور است» و «امکان حذف
اکانت نیست».

### Fixed
- 🔘 **دکمه‌های ادد هیچ پاسخی نمی‌دادند.** هر دو endpoint پس از ۹۰ ثانیه
  پاسخ خالی می‌دادند (با curl تأیید شد).

  علت: `trigger_parallel_add` تابع `collect_ready_accounts()` را مستقیم
  در هندلر HTTP صدا می‌زد. آن تابع برای هر اکانت سشن را از دیتابیس
  بازیابی و بازرسی می‌کند — با ۸ اکانت ده‌ها عملیات دیسک/DB. مسیر
  درخواست تا پایان کار بلاک می‌ماند و مینی‌اپ تایم‌اوت می‌خورد.

  حالا با `asyncio.to_thread` داخل کوروتین پس‌زمینه اجرا می‌شود و
  درخواست بلافاصله برمی‌گردد.

- 🔒 هر دو مسیر ادد اجرای همزمان را رد می‌کنند (دو عملیات روی یک سشن
  = AUTH_KEY_DUPLICATED و سوختن سشن)
- 📣 خطای پس‌زمینه دیگر بی‌صدا نیست و در همان صفحه نمایش داده می‌شود

### Added
- 🗑️ **حذف اکانت** — `POST /api/accounts/delete` و دکمه در هر کارت اکانت.
  رکورد دیتابیس، فایل سشن و فایل‌های جانبی (journal/wal/shm) پاک می‌شوند.
  اکانت مشغول قابل حذف نیست. کش لیست اکانت‌ها باطل می‌شود.
- ➕ **راهنمای افزودن اکانت** — چون کد تأیید تلگرام و رمز دو مرحله‌ای
  نباید در مرورگر وارد شوند، دکمه کاربر را به فلوی واقعی داخل ربات
  هدایت می‌کند (مدیریت اکانت‌ها ← افزودن اکانت).
- 🛡️ `tests/test_miniapp_actions.py` — ۱۶ تست

### Notes
- تست‌ها: ۹۷ → **۱۱۳** (همه سبز)

## [1.4.3] — 2026-08-16 — 🔍 پیش‌فیلتر + تشخیص اکانت

### Added
- 🔍 **پیش‌فیلتر کاربران غیرقابل‌ادد** (`add_engine.prefilter_unaddable`)
  قبل از شروع حلقه ادد، با درخواست دسته‌ای `get_users` (۱۰۰تایی)
  حساب‌های حذف‌شده، ربات‌ها و شناسه‌های ناموجود کنار گذاشته می‌شوند.

  چرا مهم است: هر تلاش ناموفق ادد بودجه نرخ اکانت را می‌سوزاند. با
  ۲۵ هزار ممبر، این یعنی هزاران درخواست بیهوده و FloodWait سریع.

  **محافظه‌کار است:** اگر بررسی شکست بخورد، همه نگه داشته می‌شوند —
  یک تلاش اضافه بهتر از حذف کاربر سالم است.

- 🩺 **`GET /api/diagnose`** — گزارش دقیق اینکه چرا هر اکانت انتخاب
  نمی‌شود: قفل مشغولی، محدودیت و زمان باقی‌مانده، وضعیت سشن،
  آخرین خطا، و وضعیت اسکن خودکار.

### Notes
- تست‌ها: ۸۲ → **۹۷** (همه سبز)

## [1.4.2] — 2026-08-16 — 🎯 آمار ادد واقعی شد + حفاظت از سهمیه اکانت

**مشکل گزارش‌شده:** «می‌نویسد ۱۰۰ نفر ادد شدند ولی داخل گروه فقط ۵ نفر هستند.»

**علت ریشه‌ای — یک باگ زیررشته‌ای:**
Pyrogram وضعیت عضویت را enum برمی‌گرداند و `str()` آن می‌شود
`ChatMemberStatus.LEFT`. کد تأیید چنین بود:

```python
if any(tok in st for tok in ("member", "administrator", ...))
```

و رشته `chatmemberstatus.left` شامل زیررشته **`member`** است!
پس هر کاربری که گروه را ترک کرده یا بن شده بود، «عضو» شمرده می‌شد.

نتیجه دوگانه بود:
1. آمار ادد کاملاً دروغ می‌شد
2. سهمیه اکانت الکی مصرف می‌شد → اکانت‌ها زودتر FloodWait می‌خوردند

### Fixed
- 🎯 **`_normalize_member_status()`** — وضعیت را از `.value`/`.name` enum
  می‌خواند، نه از `str()`. دیگر تطبیق زیررشته‌ای انجام نمی‌شود.
- 🎯 لیست صریح `_JOINED_STATUSES` و `_NOT_JOINED_STATUSES`
- ⚡ وقتی تلگرام قطعاً می‌گوید `left`/`banned`، دیگر retry نمی‌شود
  (سریع‌تر + فشار کمتر روی API = احتمال FloodWait کمتر)
- 🔒 **سه مسیر ادد دیگر که هیچ تأییدی نداشتند** حالا `confirm_joined`
  دارند: ادد تکی (خط ~۱۶۵۹)، ادد دسته‌ای (~۱۸۷۹)، ادد کانال (~۷۵۹۷).
  قبلاً فقط مسیر «ادد موازی» تأیید داشت.
- 📊 گزارش‌ها حالا «واقعاً عضو شد» را از «دعوت شد ولی عضو نشد» جدا
  می‌کنند و توضیح می‌دهند که دومی سهمیه مصرف نکرده است.

### Added
- 🛡️ **`tests/test_add_accuracy.py`** — ۲۳ تست، شامل یک تست ساختاری که
  تضمین می‌کند **هر** فراخوانی `InviteToChannel` در `bot.py` ظرف ۱۲ خط
  بعدش `confirm_joined` داشته باشد. همین تست بود که مسیر چهارم
  فراموش‌شده (ادد کانال) را پیدا کرد.

### Notes
- تست‌ها: ۵۹ → **۸۲** (همه سبز)
- گروه مقصد به `@gament_super_gp` اصلاح شد (قبلاً `selethon` مانده بود)

## [1.4.1] — 2026-08-16 — 🍯 توقف آزار اعضای گروه توسط هانی‌پات

**مشکل گزارش‌شده:** ربات هر ۱۰ دقیقه پیامی با کد `hp_trap_123456` در گروه
می‌کاشت. اعضا فکر کردند گروه هک یا اسکم شده و بسیاری گروه را ترک کردند.

**علت فنی:** کاراکترهای zero-width فقط *دورِ* متن قرار داشتند، ولی خودِ
توکن داخل تگ `<code>` بود و برای همه کاملاً قابل مشاهده. یعنی تله اصلاً
نامرئی نبود.

### Fixed
- 🍯 **هانی‌پات پیش‌فرض خاموش شد** (`HONEYPOT_MODE=off`).
  تشخیص اسکرپر با روش‌های بی‌آزار ادامه دارد: کپچا، فیلتر سن اکانت،
  تشخیص خروج سریع، امتیازدهی پروفایل مشکوک.
- 🐛 **بن‌شدن کاربران بی‌گناه:** قبلاً *هر* پیامی که رشته `hp_trap_` داشت
  باعث بن فوری می‌شد. کاربری که کد را کپی می‌کرد یا می‌پرسید «این چیه؟»
  بن می‌شد. حالا فقط توکن‌هایی که واقعاً کاشته‌ایم اعتبار دارند و
  **بن خودکار حذف شد** — فقط امتیاز ریسک + گزارش به مالک.
- 🐛 **الگوی کالبک بیش‌ازحد باز:** شرط `"trap" in data` هر دکمه‌ای با
  کلمه trap را بن می‌کرد. حالا `startswith("hp_trap_")`.
- 🧹 پیام «تایید شد، خوش آمدید» بعد از ۲۰ ثانیه پاک می‌شود تا گروه شلوغ نشود.
- 💬 متن کپچا دوستانه‌تر شد و توضیح می‌دهد که یک بررسی خودکار است.

### Changed
- اگر `HONEYPOT_MODE=invisible_link` شود، تله فقط یک لینک با انکر خالی
  (`\u2060`) است — هیچ متن قابل خواندنی ندارد — و بعد از ۳ ثانیه
  (قبلاً ۱۵ ثانیه) حذف می‌شود.

### Added
- 🛡️ **`tests/test_defender_safety.py`** — ۷ تست که تضمین می‌کنند:
  تله پیش‌فرض خاموش است، در حالت خاموش هیچ پیامی به گروه نمی‌رود،
  متن تله هیچ کاراکتر قابل‌خواندنی ندارد، اشاره به توکن باعث بن نمی‌شود،
  و هشدارها هرگز در گروه منتشر نمی‌شوند.
- `HONEYPOT_MODE` در `.env.example` و `render.yaml`

### Notes
- تست‌ها: ۵۲ → **۵۹** (همه سبز)
- `pytest-asyncio` به وابستگی‌های توسعه اضافه شد

---

## [1.4.0] — 2026-08-16 — 🧹 بازگشت به دامنه اصلی: فقط اسکرپر و ادد ممبر

پروژه از ادغام‌های ناخواسته با ریپوهای دیگر پاک‌سازی شد. دامنه ربات دوباره
همان چیزی است که باید باشد: **استخراج ممبر و ادد به گروه خودی**.

### Fixed
- 🐛 **باگ خاموشی دفاع گروه:** دو هندلر روی `filters.text & filters.group`
  در یک group پیش‌فرض ثبت شده بودند. در Pyrogram فقط اولی اجرا می‌شود، پس
  `defender.monitor_message` (فیلتر سن اکانت) **هیچ‌وقت صدا زده نمی‌شد**.
  هندلر `mon` به `group=1` منتقل شد تا هر دو کار کنند.

### Removed
- 🗑️ **چت‌بات Flexa** (۲۹۳ خط از `bot.py` + ۴ ماژول):
  `ai_chat.py`, `flexa_integration.py`, `flexa_auto_respond.py`, `chat_history.py`.
  دو هندلر آن هم به‌خاطر همان باگ بالا عملاً مرده بودند.
- 🗑️ **`hunter.py`** — کد ریپوی جداگانه `crypto-treasure-hunter`، هیچ ارجاعی نداشت
- 🗑️ **`project_finder.py`** — اسکنر پروژه گیت‌هاب، بی‌ربط و بدون ارجاع
- 🗑️ **`downloader.py`** — دانلودر مدیا، بی‌ربط و بدون ارجاع
- 🗑️ **کد مرده:** `simple_flow.py`, `patch.py`, `patch_callbacks.py`, `ultimate_accounts.py`

### Added
- 🛡️ **`tests/test_scope_clean.py`** — ۵ تست نگهبان که جلوی برگشت این ماژول‌ها
  را می‌گیرند، وجود کد «دادگاه/داوری» را رد می‌کنند، قاطی‌شدن
  `python-telegram-bot` با Pyrogram را می‌گیرند و باگ تداخل هندلرها را
  دوباره پیدا می‌کنند.

### Notes
- `chat_analyzer.py` **نگه داشته شد** — برای دسته‌بندی موضوعی گروه‌های اسکرپ‌شده
  استفاده می‌شود (نه چت‌بات).
- `lead_finder.py` **نگه داشته شد** — تابع `search_telegram_groups_by_topic`
  برای پیدا کردن گروه‌های منبع اسکرپ به کار می‌رود.
- تست‌ها: ۴۲ → **۴۷** (همه سبز)

---

## [1.3.3] — 2026-08-13 — 🔬 تست زنده واقعی به‌جای برچسب سالم

### Changed
- «سالم» فقط بعد از تست زنده اتصال (get_me + دیالوگ‌ها)
- سشن بدون user_id = لاگین ناتمام → از ادد کنار گذاشته می‌شود
- اسکن خودکار بعد از استارت، اکانت‌های صفر-ادد را زنده تست می‌کند

---

## [1.3.2] — 2026-08-13 — ▶️ اکانت‌های صفر-ادد مثل بقیه وارد کار می‌شوند

### Changed
- اکانت‌های `0/100` دیگر «بی‌استفاده» نیستند — مثل بقیه «سالم» و آماده ادد/استخراج‌اند
- ادد موازی همه اکانت‌های سالم (حتی صفر-ادد) را جمع می‌کند
- بازیابی خودکار سشن از بکاپ ابری موقع استارت و قبل از ادد

---

## [1.3.1] — 2026-08-13 — 🔍 تشخیص اکانت‌های «سالم ولی بی‌استخراج»

### Added
- ماژول `account_state` — قفل اشغال بودن اکانت (جلوگیری از سوختن سشن با استفاده همزمان)
- ماژول `account_doctor` — تشخیص آفلاین + تست زنده اتصال/هویت/لیست‌چت/استخراج
- دکمه «تشخیص سلامت اکانت‌ها» در منوی مدیریت + گزارش سریع همه
- چرخش عادلانه اسکن خودکار بین **همه** اکانت‌های آزاد (نه فقط یکی)
- وضعیت‌های واقعی در مینی‌اپ: بدون سشن / مشغول / بی‌استفاده

### Fixed
- اسکن خودکار و اسکرپ مینی‌اپ همیشه اولین/تنها اکانت را برمی‌داشتند → سه اکانت «سالم» هیچ‌وقت نوبت استخراج نمی‌گرفتند
- برچسب «سالم» فقط یعنی FloodWait ندارد؛ دیگر گمراه‌کننده نیست
- استفاده همزمان از یک سشن (اسکن + ادد) که AUTH_KEY را می‌سوزاند

---

## [1.3.0] — 2026-08-13 — 🏗️ بسته حرفه‌ای‌سازی

### Added
- ماژول پیکربندی مرکزی `config.py` (env / .env / مقادیر پیش‌فرض)
- سیستم لاگینگ استاندارد `logging_setup.py` (کنسول + فایل چرخشی + شکار print ها)
- ماژول مستقل موتور ادد `add_engine.py` (تاخیر انسانی + کش لیست ممنوعه)
- پوشه `tests/` با ۲۵ تست خودکار (کانفیگ، موتور ادد، رمزنگاری، پول DB، مینی‌اپ، کامپایل)
- CI با GitHub Actions (`.github/workflows/ci.yml`)
- `Dockerfile` + `.dockerignore` برای استقرار تولیدی
- `Makefile` (run / test / lint / check / ci / docker)
- `pyproject.toml`، `LICENSE` (MIT)، `.env.example`، `CHANGELOG.md`، `docs/agent-notes.md`

### Changed
- پین کردن نسخه‌های دقیق `requirements.txt` + `requirements-dev.txt`
- یکپارچه‌سازی اعتبارنامه‌های API در `config.py` (رفع ناسازگاری API_ID/API_HASH بین bot و bg_scraper)
- README بازنویسی شد (بج‌ها، معماری، جدول متغیرها، راهنمای توسعه)

### Fixed
- 🐛 باگ `atk_state_ref` تعریف‌نشده → آمار زنده مینی‌اپ در فلوی ادد موازی کار نمی‌کرد
- 🐛 باگ `_db_count_users` (نام تعریف‌نشده `count_users`) → منوی اصلی همیشه مسیر کند (لود کامل کاربران) را طی می‌کرد
- 🐛 باگ `ChatPermissions` تعریف‌نشده در هندلرهای mute/unmute
- 🐛 f-string شکسته در منوی اینستاگرام (`شده{ها}`) — کرش در شاخه follow
- 🐛 `PeerIdInvalid` تعریف‌نشده در فلوی ادد CSV
- حذف `chat_history.db` از گیت (فایل دیتابیس اجرایی نباید در مخزن باشد)

### Security
- رمزنگاری سشن‌ها ارتقا یافت: **AES-CTR با nonce تصادفی + HMAC-SHA256** (فرمت `SES3`)
  با مهاجرت خودکار از فرمت قدیمی و داده خام
- `init_tables()` در import محافظت شد + تلاش مجدد ۵ مرحله‌ای در استارت بات

---

## [1.2.0] — 2026-08-13 — ⚡ بهینه‌سازی عملکرد

### Added
- پول اتصال دیتابیس (`ThreadedConnectionPool`) با کرسورهای پروکسی آزادکننده خودکار

### Changed
- چک‌های ضد-تکرار از ۳ کوئری DB به ازای هر ممبر → ست در حافظه (~۱۱۰۰ برابر سریع‌تر)
- کش ۲ ثانیه‌ای APIهای مینی‌اپ در حالت بیکار (زنده در حین ادد)
- `build_welcome_text` از لود کامل ۲۵هزار کاربر → `COUNT(*)`
- `workers` ربات: ۱ → ۴
- فلوی CSV: حذف پارس دوباره فایل

---

## [1.1.0] — 2026-08-13 — 🛡️ ضد بن + ضد تکرار

### Added
- جدول `do_not_add_tbl` — لیست «هرگز دوباره ادد نشود»
- تاخیرهای انسانی با نویز تصادفی + استراحت‌های دوره‌ای
- کنسول زنده عملیات در مینی‌اپ + استریپ وضعیت اکانت‌ها
- بلاک‌لیست خودکار کاربرانی که لفت می‌دهند

### Changed
- پرایوسی‌بسته‌ها به‌جای «خطا» → «اسکیپ» شمارش می‌شوند + حذف از دیتابیس
- سقف روزانه Ultra: ۱۰۰ → ۵۰

### Fixed
- باگ المان `m-limited` (رفرنس JS بدون وجود در HTML) — آپدیت زنده داشبورد شکسته بود
- هندلر لفت، هندلر مدافع را سایه می‌کرد (`continue_propagation`)

---

## [1.0.0] — 2026-08-12 — 🚀 مینی‌اپ و معماری اصلی

### Added
- Telegram Mini App (RTL فارسی) با داشبورد زنده
- ادد تک‌اکانت و ادد موازی با ۳ حالت سرعت (Safe / Fast / Ultra)
- کنسول سلامت اکانت‌ها (ظرفیت روزانه، FloodWait)
- ریست خودکار ۲۴ ساعته شمارنده‌ها
- Neon PostgreSQL با Auto-Reconnect و `@db_retry`
- ایندکس‌های چندستونه برای ۲۵,۰۰۰+ ممبر

---

## [0.x] — قبل از 2026-08 — هسته اسکرپ

- ۱۲ متد استخراج ممبر (پجینیشن، هیستوری، رویدادهای join، ری‌اکشن و…)
- مدافع گروه (کپچا، هانی‌پات، فیلتر سن اکانت)
- مدیریت اکانت‌ها با بکاپ/بازیابی سشن
